let possibleDiv = document.getElementById("possible");
let guessDiv = document.getElementById("guess");

// chrome.storage.local.get("nyt-wordle-state", ({ "nyt-wordle-state": val }) => {
//   possibleDiv.innerHTML = val;
// });

// chrome.storage.local.set({ foo: "bar" });
// chrome.storage.local.get(["nyt-wordle-state"], function (result) {
//   alert("Number of keys is " + Object.keys(result).length);
// });
